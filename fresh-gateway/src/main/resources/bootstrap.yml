spring:
  application:
    name: fresh-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        file-extension: yaml
        group: DEFAULT_GROUP

server:
  port: 9000
